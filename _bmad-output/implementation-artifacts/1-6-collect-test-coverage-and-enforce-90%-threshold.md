# Story 1.6: Collect test coverage and enforce 90% threshold

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a developer,
I want the build/test pipeline to gather coverage metrics and fail if overall coverage falls below 90%,
so that we maintain high-quality code and drive early test creation.

## Acceptance Criteria

1. [Add acceptance criteria from epics/PRD]
   - Given the monorepo with both frontend and backend tests,
   - When the test suite runs (unit or e2e),
   - Then coverage reports are generated for both services,
   - And the CI or local script fails with a clear error if total coverage is under 90%.

## Tasks / Subtasks

- [x] Configure Vitest and Playwright (or other test runners) to collect coverage metrics for both frontend and backend. **(vitest configs updated)**
- [x] Add thresholds/checks that aggregate coverage across services and compare against 90%. **(created scripts/check-coverage.js with aggregating logic and unit tests)**
- [x] Update CI or local test script to evaluate coverage and exit non‑zero if below threshold. **(added workspace scripts `coverage:*`, `check-coverage`, and `coverage` in root package.json)**
- [x] Document coverage enforcement steps in repository README or contributing guide. **(README updated)**

## Dev Notes

### File List

- package.json
- frontend/vitest.config.ts
- backend/vitest.config.ts
- scripts/check-coverage.js
- scripts/check-coverage.test.ts
- README.md

### Review Follow-ups (AI)

- [x] [AI-Review][High] Add File List section with all changed files to story document [file:_bmad-output/implementation-artifacts/1-6-collect-test-coverage-and-enforce-90%-threshold.md]
- [x] [AI-Review][Medium] Update `scripts/check-coverage.js` to treat missing coverage reports as failure and/or exit non-zero [file:scripts/check-coverage.js]
- [ ] [AI-Review][Medium] Extend coverage aggregation to include branch/func metrics or document that only lines are counted [file:scripts/check-coverage.js]
- [x] [AI-Review][Medium] Make threshold configurable via env var or CLI argument in root scripts [file:scripts/check-coverage.js, package.json]
- [ ] [AI-Review][Medium] Include e2e coverage in aggregation or clarify assumption [file:scripts/check-coverage.js]
- [x] [AI-Review][Low] Add tests for merging multiple reports with overlapping file paths [file:scripts/check-coverage.test.ts]
- [x] [AI-Review][Low] Document local usage of `npm run coverage` and threshold override in README [file:README.md]
- [x] [AI-Review][Low] Ensure README describes which metrics are aggregated and limitations [file:README.md]

- Use built-in Vitest coverage reporters for both frontend and backend to keep tooling simple and ensure cross-project compatibility.
- E2E tests likely contribute minimally; focus on unit/component coverage initially.
- Scripts should run from monorepo root so both packages are instrumented.

### Project Structure Notes

- The coverage check will touch root `package.json` scripts, frontend and backend test configs, and possibly CI configuration files.

### References

- Source: Requirements in epic 1 (additional requirement: "Collect test coverage and enforce 90% threshold").

## Dev Agent Record

_Story generated by create-story workflow triggered manually._

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

- n/a

### Completion Notes List

1. Story document created with clear goals, acceptance criteria, and tasks.

- `package.json` (update needed)
- `vitest.config.ts` adjustments (both services)
- CI/test script modifications
- `scripts/check-coverage.js` helper and its accompanying tests
